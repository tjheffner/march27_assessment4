<html>
    <head>
        <title>Assessment 4: Shoe Store & Brand Database</title>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    </head>
    <body>
        <div class="container">
        <h1>Shoe Brand List</h1>

        <p>Add a new brand</p>

        <form action="/brands" method="post">
            <label for="brand_name">Brand name:</label>
            <input id="brand_name" name="brand_name" type="text">

            <button type="submit">Add brand</button>
        </form>

        {% if brands is not empty %}
            <p>Here are all available brands:</p>
            <ul>
                {% for brand in brands %}
                    <li><a href="/brands/{{ brand.getId }}">{{ brand.getBrandName }}</a></li>
                {% endfor %}
            </ul>

            <form action='/delete_brands' method='post'>
                <button type='submit'>Clear</button>
            </form>

        {% else %}
            <p>There are no brands yet!</p>
        {% endif %}

            <p><a href='/'>Home</a></p>

        </div>
    </body>
</html>
